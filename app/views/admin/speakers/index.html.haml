%h1 Listing Speakers

- if @speakers.any?
  %table.zebra
    %tr
      %th Name
      %th Languages
      %th Practice Areas
      %th Actions

    - @speakers.each do |speaker|
      %tr
        %td= link_to speaker.name, [:edit, :admin, speaker]
        %td= speaker.languages_count
        %td= speaker.practice_areas_count
        %td
          = link_to 'Delete', [:admin, speaker], :confirm => 'Are you sure?', :method => :delete
          | |
          = link_to "View", speaker

  %br

- else
  %p No speakers.

= link_to 'Add Speaker', [:new, :admin, :speaker]
