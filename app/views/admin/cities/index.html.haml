%h1 Listing Cities

- if @cities.any?

  %table.zebra
    %tr
      %th Name
      %th Country
      %th Seminars
      %th Agenda?
      %th Description?
      %th Venue?
      %th Default?
      %th Actions

    - @cities.each do |city|
      %tr
        %td= link_to city.name, [:edit, :admin, city]
        %td= city.country
        %td= city.seminars.count
        %td= city.agenda.present? ? 'yes' : 'no'
        %td= city.description.present? ? 'yes' : 'no'
        %td= city.venue.present? ? 'yes' : 'no'
        %td= city.default_city.present? ? 'yes' : 'no'
        %td
          = link_to "Manage Seminars", [:admin, city, :seminars]
          | |
          = link_to "View", [city]
          | |
          = link_to 'Delete', [:admin, city], :confirm => 'Are you sure?', :method => :delete
  %br

- else
  %p No cities.  

= link_to 'Add City', [:new, :admin, :city]