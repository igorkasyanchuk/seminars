= error_messages_for :presentation

= simple_form_for [:admin, @panel, @presentation], :html => {:multipart => true} do |f|

  = f.input :title
  = f.input :description, :input_html => { :rows => 6, :cols => 60 }
  = f.input :file

  = f.button :submit