= content_for :on_ready do
  $('#seminar_details').tab('show');
  console.log(window.location.hash);
  if (window.location.hash == '#registation') $('ul.nav-tabs li:last a').trigger('click');

%h1.with_spacer.title.left #{@seminar.title}
= link_to 'Registration', '#registration', :class => 'btn btn-large btn-danger register_button agenda_button_register_top_seminar_page'

.clear

#seminar_details.tabbable
  %ul.nav.nav-tabs
    %li.active
      %a{"data-toggle" => "tab", :href => "#s1"} Agenda
    %li
      %a{"data-toggle" => "tab", :href => "#s2"} Speakers
    %li
      %a{"data-toggle" => "tab", :href => "#s3"} Sponsors
    %li
      %a{"data-toggle" => "tab", :href => "#s4"} Registation

  .tab-content
    #s1.tab-pane.active
      - @seminar.panels.by_start_at.each_with_index do |panel, index|
        = render :partial => '/seminars/panel', :locals => { :panel => panel }
        - if index != @seminar.panels.count - 1
          = line "", "no_line"
      .clear

      = @seminar.agenda.html_safe

      = link_to 'Registration', '#registration', :class => 'btn btn-large btn-danger register_button agenda_button_register_bottom'

    #s2.tab-pane
      %ul.with_discs
        - @seminar.speakers.each do |speaker|
          %li
            = link_to speaker.name, speaker

    #s3.tab-pane
      %ul.with_discs
        - @seminar.sponsors.each do |sponsor|
          %li
            = link_to sponsor.name, sponsor
        
    #s4.tab-pane
      = @seminar.tickets_html.html_safe


